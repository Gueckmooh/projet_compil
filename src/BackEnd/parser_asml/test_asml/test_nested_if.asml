let _ =
    let a = 9 in
    let b = if a >= 2 then (
        if a >= 3 then (
           33
        ) else (
           4
        )
    ) else (
        2
    ) in
    let c = add a b in
    call _min_caml_print_int c